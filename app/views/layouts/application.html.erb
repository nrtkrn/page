<!DOCTYPE html>
<html>
  <head>
    <title>Page</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <div class="c_head a_flex">
        <h1>Page</h1>
        <%= link_to "カテゴリー一覧", root_path,class:"b_link__h" %>
        <%= link_to "レシピをよむ", root_path,class:"b_link__h" %>
        <%= link_to "日付を進める", root_path,class:"b_link__h" %>

        <%= form_tag(root_path,:method => 'get') do %>
          <%= text_field_tag :dammy, '', class: 'c_form', placeholder: '料理名・食材でレシピをさがす' %>
        <% end %>
      </div>
    </header>

    <% if true %>
      <div class="c_box__announce">
        <div class="c_box__announce_inner c_fadein">
          <%= form_with url: root_path, method: :get do |f| %>
            <% 3.times do %>
              <table class="table c_table__announce">
                <tbody>
                  <tr>
                    <th>
                      <div>
                        <%= link_to(root_path, class:"a_link__black") do %>
                          <%= image_tag 'cake.jpg', size: '100x100',class:"c_photo__rank" %>
                          <p>メニュー名</p>
                        <% end %>
                      </div>
                    </th>
                    <td>
                      <div class="c_field__announce">
                        <%= f.select :dammy, ["調理済","未調理"] %>
                      </div>
                    </td>
                  </tr>
                </tbody>
              </table>
            <% end %>
            <div class="a_actions a_pointer c_submit__announce">
              <%= f.submit "確定" %>
            </div>
          <% end %>
        </div>
      </div>
    <% end %>

    <body>
      <%= yield %>
    </body>

    <footer>
      <div class="c_foot">
       <%= link_to "概要", root_path,class:"c_link__f" %>
      </div>
    </footer>
  </body>
</html>
